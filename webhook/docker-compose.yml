services:
  webhook:
    build: 
      context: ./docker
      dockerfile: Dockerfile
    container_name: webhook
    command: -verbose -hooks=/webhook/config/hooks.yaml -hotreload
    ports:
      - 9000:9000
    volumes:
      - ./volumes/config:/webhook/config
      - ./volumes/scripts:/webhook/scripts

